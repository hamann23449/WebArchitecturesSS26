{
  "name": "album-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node index.js",
    "dev": "npm i && npm run prisma:generate && npm run prisma:migrate && nodemon index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init"
  },
  "dependencies": {
    "@prisma/client": "^5.9.0",
    "bcrypt": "^5.1.0",
    "better-sqlite3": "^12.9.0",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "prisma": "^5.9.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "vitest": "^4.1.5"
  }
}
